// Re-export shared types for convenience
export type {
  PlayerState,
  ChatMessage,
  GameState,
  ClientMessage,
  ServerMessage,
  JoinResponse,
  SessionData,
  PlayerRecord,
} from '@wol/shared'
