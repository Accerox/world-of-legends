apiVersion: v1
kind: Service
metadata:
  name: wol-game-server
  namespace: wol-game
spec:
  selector:
    app: wol-game-server
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
